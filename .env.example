# Telnet WebSocket Proxy Configuration

# Port for the WebSocket server (default: 8888)
PORT=8888

# Port for the health check HTTP endpoint (optional - if not set, health server is disabled)
# HEALTH_PORT=8889

# Heartbeat interval in milliseconds (default: 5000)
HEARTBEAT_INTERVAL=5000

# Telnet connection timeout in milliseconds (default: 30000)
TELNET_TIMEOUT=30000

# Parser buffer size limit in bytes (default: 1048576 = 1MB)
PARSER_BUFFER_SIZE=1048576